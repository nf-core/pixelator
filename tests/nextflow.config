/*
========================================================================================
    Nextflow config file for running tests
========================================================================================
*/

params {
    // Default should point to a specific commit on github.com/nf-core/test-datasets, branch pixelator
    pipelines_testdata_base_path = System.getenv('NFT_TESTDATA_BASE_PATH') ?: "https://raw.githubusercontent.com/elhb/nf-core-test-datasets/8c61c1f4d643b8be161f919ff8382a12bfc11073/"
    modules_testdata_base_path   = "https://raw.githubusercontent.com/nf-core/test-datasets/modules/data/"
    outdir                       = 'results'
}

aws.client.anonymous = true // fixes S3 access issues on self-hosted runners
