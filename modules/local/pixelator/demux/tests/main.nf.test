nextflow_process {

    name "Test Process PIXELATOR_DEMUX"
    script "../main.nf"
    process "PIXELATOR_DEMUX"
    tag "modules"
    tag "pixelator"
    tag "pixelator/demux"

    test("PNA demux - stub") {

        options "-stub"

        when {
            params {
                skip_experiment_summary = true
            }
            process {
                """
                input[0] = [
                    [ id:'pool1', design:'proxiome-v2', panel:'proxiome-v2-immuno-155-v1.0', technology:'proxiome-v2' ],
                    file(params.pipelines_testdata_base_path + 'testdata/pna/modules/amplicon/pool1.amplicon.fq.zst', checkIfExists: true),
                    [],
                    'proxiome-v2-immuno-155-v1.0',
                    'proxiome-v2'
                ]
                """
            }
        }
        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }

    test("PNA demux - small test") {
        when {
            params {
                skip_experiment_summary = true
                outdir   = "$outputDir"
            }
            process {
                """
                input[0] = [
                    [ id:'pool1', design:'proxiome-v2', panel:'proxiome-v2-immuno-155-v1.0', technology:'proxiome-v2' ],
                    file(params.pipelines_testdata_base_path + 'testdata/pna/modules/amplicon/pool1.amplicon.fq.zst', checkIfExists: true),
                    [],
                    'proxiome-v2-immuno-155-v1.0',
                    'proxiome-v2'
                ]
                """
            }
        }
        then {
            def stable_name = getAllFilesFromDir(params.outdir, relative: true, includeDir: true, ignore: ['pipeline_info/*.{html,json,txt}'])
            assertAll(
                { assert workflow.success },
                { assert snapshot(
                    // Number of successful tasks
                    workflow.trace.succeeded().size(),
                    // All stable path name, with a relative path
                    stable_name,
                    process.out.versions_pixelator,
                ).match() }
            )
        }
    }

    test("PNA demux - multiple chunks") {
        config "./multiple_chunks.config"

        when {
            params {
                skip_experiment_summary = true
                outdir   = "$outputDir"
            }
            process {
                """
                input[0] = [
                    [ id:'pool1', design:'proxiome-v2', panel:'proxiome-v2-immuno-155-v1.0', technology:'proxiome-v2' ],
                    file(params.pipelines_testdata_base_path + 'testdata/pna/modules/amplicon/pool1.amplicon.fq.zst', checkIfExists: true),
                    [],
                    'proxiome-v2-immuno-155-v1.0',
                    'proxiome-v2'
                ]
                """
            }
        }

        then {
            def stable_name = getAllFilesFromDir(params.outdir, relative: true, includeDir: true, ignore: ['pipeline_info/*.{html,json,txt}'])
            assertAll(
                { assert workflow.success },
                { assert snapshot(
                    // Number of successful tasks
                    workflow.trace.succeeded().size(),
                    // All stable path name, with a relative path
                    stable_name,
                    process.out.versions_pixelator,
                ).match() }
            )
        }
    }
}
